# openclaw-scan

Read-only CLI that discovers OpenClaw workspaces and produces sanitized snapshot JSON.

## Planned

- `openclaw-scan --token <TOKEN>` — scan and upload
- `--path`, `--export`, `--include-runs`
- Auto-detect workspace, parse agents/skills/MCP/tools, never extract secrets
